<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> </title>
  <link rel="stylesheet" href="/css/navbar.css">
  <link rel="stylesheet" href="/css/site.css">
  <link rel="stylesheet" href="/css/table.css">
</head>
<body>
  <%- include('header.ejs', nav)%>
  <main>
    <div id="postButtonP"><a href="/user/createPost" class="a-button">Post a blog</a></div>
    <% if (posts.length > 0) { %>
      <table>
        <thead>
          <tr>
            <th>Posts</th>
            <th></th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% for( let i = 0; i < posts.length; i++ ) { %>
           <tr>
             <td><%= posts[i].title %></td>
             <td><a href="/user/editPost/<%= posts[i]._id %>" class="link">Edit</a></td>
             <td><a href="/user/deletePost/<%= posts[i]._id %>" class="link">Delete</a></td>
             <td><a href="/posts/<%= posts[i]._id %>" class="link">Read Blog</a></td>
           </tr>
          <% } %>
        </tbody>
      </table>
     <% } else { %>
       <div class="info-bar">
         <h2>Nothing here. Try posting something...</h2>
       </div>
     <% } %>
  </main>
</body>
</html>

